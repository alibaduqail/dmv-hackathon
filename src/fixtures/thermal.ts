import type { ThermalCapture } from '../types.ts';

// Phase 6 fills this, from the lunch capture. Two committed PNGs under
// src/fixtures/thermal/. Temperatures read off the vendor app, hard-coded.
// There is no capture code in this repo and there will not be.
export const captures: ThermalCapture[] = [];
