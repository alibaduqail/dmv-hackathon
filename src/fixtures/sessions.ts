import type { Session } from '../types.ts';

// Phase 1 fills this. Seven sessions, Jun 13 -> Jul 25 2026.
// FROZEN AT 11:00. Read .claude/skills/seed-fixtures/SKILL.md first.
export const sessions: Session[] = [];

/** The session under review in the demo. */
export const CURRENT_SESSION_ID = 'session-07';
export const CURRENT_TARGET = '/r/ initial';
